import setuptools

setuptools.setup(
    name="automated_critical_edition",  # Replace with your own username
    version="0.0.1",
    packages=setuptools.find_packages(where='automated_critical_edition'),
)
